# Integration tests

Planned integration coverage:

- Install/uninstall/upgrade SQL on PrestaShop 8.0, 8.1, 8.2.
- Repository reads/writes with `_DB_PREFIX_`.
- Multishop configuration values.
- Mail template path resolution through `Mail::Send`.
- Experimental PrestaShop 9 install and admin route loading.

